# Training / analysis pipeline dependencies (src/, scripts/, tests/).
#
# For the inference/scoring container, use submission/requirements.txt
# instead -- that file is the byte-pinned, build-time-verified set (it also
# pins torch==2.7.1 / timm==1.0.22 against a specific CUDA index for the
# T4/A10G/Blackwell targets; see its own comments).
#
# The versions below were captured with `pip freeze` on the actual
# development/training container on 2026-08-27 -- not guessed -- but unlike
# submission/requirements.txt they were not independently re-verified as the
# exact versions in effect for every historical run in reports/, since no
# environment lockfile was captured at training time. Treat this file as a
# faithful snapshot of a working environment, not a build-time-verified pin.
numpy==2.3.5
scipy==1.15.3
opencv-python-headless==4.11.0.86
pillow==12.2.0
pandas==2.3.3
scikit-learn==1.8.0
PyYAML==6.0.3
tqdm==4.67.1
matplotlib==3.10.8
tifffile==2026.3.3
SimpleITK==2.5.3
psutil==7.1.3
Flask==3.1.3
pytest==8.1.1
diptest==0.11.0

# imagehash: imported by scripts/06_regroup.py but NOT installed in the
# development container as of this capture (2026-08-27) -- its version
# could not be verified from the actual environment. Install the current
# PyPI release and re-pin once confirmed against the version actually used
# to produce group_id / near_duplicate_clusters.csv.
imagehash
